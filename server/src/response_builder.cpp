#include "../includes/response_builder.h"
Snapshot ResponseBuilder:: buildSnapshot(const std::map<Id,Player>& players, const World& world) {
    
    for (auto& [id, player]: players) {
       
    }
}

ResponseBuilder::~ResponseBuilder()
{
}
